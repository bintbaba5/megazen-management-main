import { SignIn } from "@/components/sign-in";
import React from "react";

const page = () => {
  return (
    <div className="container max-w-full flex justify-center items-center h-screen">
      <SignIn />
    </div>
  );
};

export default page;
